# ThinkGrasp runtime requirements for Python 3.12 + newer CUDA/PyTorch.
#
# Install torch, torchvision and torchaudio separately first. They must match
# your GPU driver/CUDA runtime, for example the CUDA 13 / RTX 50-series build.
# Do not let this file replace your working torch 2.x + CUDA 13 installation.

# Core numerical stack. NumPy is pinned below 2.0 because GraspNet/open3d-era
# robotics packages and some legacy project utilities are still safest on the
# NumPy 1.x ABI. OpenCV is kept at 4.10 for the same reason.
numpy==1.26.4
scipy==1.15.3
scikit-learn==1.6.1
pandas==2.2.3
numba==0.61.2
h5py==3.12.1

# Vision, geometry and robotics runtime.
opencv-python==4.10.0.84
Pillow>=10.2,<13.0
matplotlib==3.10.7
imageio>=2.34,<3.0
open3d==0.19.0
open3d-plus==0.3.4
pybullet==3.2.7
pyquaternion==0.9.9
trimesh>=4.3,<5.0
Shapely>=2.0,<3.0
object2urdf==0.12
pycocotools>=2.0.7,<3.0
transforms3d==0.4.2
autolab-core==1.1.1
autolab-perception==1.0.0
cvxopt==1.3.3
dill==0.4.1
grasp-nms==1.0.2
pywavefront==1.3.3
scikit-image==0.25.2
tifffile==2024.8.30
# Install GraspNetAPI separately after this file:
#   python -m pip install --no-deps graspnetAPI==1.2.10
# Its upstream dependency metadata pins transforms3d==0.3.1, which does not
# build cleanly on Python 3.12. The runtime API works with transforms3d 0.4.2.

# Experiment, logging and utility packages.
tqdm>=4.66,<5.0
wandb>=0.16,<0.23
tensorboard>=2.16,<3.0
tensorboardX>=2.6,<3.0
thop>=0.1.1.post2209072238
termcolor>=2.4,<4.0
graphviz>=0.20,<1.0
memory-profiler>=0.61,<1.0
addict==2.4.0
packaging>=24,<27
PyYAML>=6.0.1,<7.0
protobuf>=3.20.3,<5.0
requests>=2.31,<3.0
urllib3>=1.26,<3.0
ftfy>=6.1,<7.0
regex>=2023.10.3
psutil>=5.9,<8.0

# Runtime services and model-download helpers.
flask>=3.0,<4.0
openai>=1.35,<3.0
ray==2.52.1
transformers==4.57.3
huggingface_hub>=0.23,<1.0

# LangSAM dependencies used by langsam/langsam_actor.py. PyPI packages are used
# here so `pip install -r requirements.txt` can work without Git build-isolation
# failures before PyTorch is visible. If you need upstream Git versions, install
# them manually after torch with `pip install --no-build-isolation git+...`.
groundingdino-py==0.4.0
segment-anything==1.0

# Build helpers for local CUDA extensions.
Cython>=3.0,<4.0
ninja>=1.11,<2.0

# Optional VLPart/GLIP dependencies are intentionally not listed. Detectron2 and
# GLIP still depend on the exact torch/CUDA build; realarm.py falls back to
# LangSAM when that optional stack or its checkpoints are unavailable.
